# Ktor HTTP engine (merged on classpath with common `application.conf` `restaurant.*` defaults).
# Deployment port follows `restaurant.server.port` so ConfigLoader and Netty stay aligned.

ktor {
  deployment {
    host = "0.0.0.0"
    port = ${restaurant.server.port}
  }
}
